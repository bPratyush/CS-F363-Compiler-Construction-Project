begin program:
begin VarDecl:
(a, int)
(b, int); (c, int);
(b, char);
// list of variables ended.
end VarDecl
b := (20, 10);
print("Welcome !!!");
scan("@, @", a, b);
scan("@, @", b);
end program